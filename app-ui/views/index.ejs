<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <title> TProyecto Final SD2020-1 </title>

    <!-- BOOTSTRAP 4 CDN-->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>

    <nav class="navbar-darl bg-dark mb-4">
        <a href="/" class="navbar-brand">Final Project Web Server</a>
    </nav>
    <div class="container">
        <div class="row">
            <!-- FORM-->
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/add" method="POST">
                            <div class="form-group">
                                <input type="text" name="name" placeholder="Inserte un nombre" class="form-cotrol">
                            </div>
                            <button type="submit" class="bin btn-primary btn-block">
                                Agregar
                            </button>
                        </form>
                    </div>
                </div>

            </div>
            <div class="row">
                <% person.forEach(function(person, index) { %>
                    <div class="panel-footer small">
                        <p>$ <%= person.name %>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
